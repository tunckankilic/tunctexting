import 'package:flutter/material.dart';

const backgroundColor = Color.fromRGBO(19, 28, 33, 1);
const textColor = Color(0xFF1505f7);
const appBarColor = Color.fromRGBO(31, 44, 52, 1);
const webAppBarColor = Color.fromRGBO(42, 47, 50, 1);
const messageColor = Color.fromRGBO(5, 96, 98, 1);
const senderMessageColor = Color.fromRGBO(37, 45, 49, 1);
const tabColor = Color.fromRGBO(0, 167, 131, 1);
const searchBarColor = Color.fromRGBO(50, 55, 57, 1);
const dividerColor = Color.fromRGBO(37, 45, 50, 1);
const chatBarMessage = Color.fromRGBO(30, 36, 40, 1);
const mobileChatBoxColor = Color.fromRGBO(31, 44, 52, 1);
const greyColor = Colors.grey;
const blackColor = Colors.black;

class AppColors {
  // Main Themes
  static const Color primaryBackground = Color.fromRGBO(19, 28, 33, 1);
  static const Color secondaryBackground = Color.fromRGBO(31, 44, 52, 1);
  static const Color tertiaryBackground = Color.fromRGBO(37, 45, 49, 1);

  // Text Colors
  static const Color primaryText = Color(0xFF1505f7);
  static const Color secondaryText = Color(0xFFE0E0E0);
  static const Color subtleText = Color(0xFFAAAAAA);

  // App Bar Colors
  static const Color mobileAppBar = Color.fromRGBO(31, 44, 52, 1);
  static const Color webAppBar = Color.fromRGBO(42, 47, 50, 1);

  // Message Colors
  static const Color outgoingMessage = Color.fromRGBO(5, 96, 98, 1);
  static const Color incomingMessage = Color.fromRGBO(37, 45, 49, 1);

  // Component Colors
  static const Color tabHighlight = Color.fromRGBO(0, 167, 131, 1);
  static const Color searchBar = Color.fromRGBO(50, 55, 57, 1);
  static const Color divider = Color.fromRGBO(37, 45, 50, 1);
  static const Color chatInputBox = Color.fromRGBO(30, 36, 40, 1);
  static const Color mobileChatBox = Color.fromRGBO(31, 44, 52, 1);

  // General Colors
  static const Color grey = Colors.grey;
  static const Color black = Colors.black;

  // Extra Colors
  static const Color accentGreen = Color(0xFF00A884);
  static const Color linkBlue = Color(0xFF53BDEB);
  static const Color warningYellow = Color(0xFFFFD600);
  static const Color errorRed = Color(0xFFFF3B30);

  // Status Colors
  static const Color onlineStatus = Color(0xFF4AD504);
  static const Color offlineStatus = Color(0xFFFF8C00);

  // Shadows
  static const Color shadowColor = Color.fromRGBO(0, 0, 0, 0.1);

  // Transparent Colors
  static const Color semiTransparentWhite = Color.fromRGBO(255, 255, 255, 0.1);
  static const Color semiTransparentBlack = Color.fromRGBO(0, 0, 0, 0.5);

  // Gradient Colors
  static const List<Color> primaryGradient = [
    Color.fromRGBO(18, 140, 126, 1),
    Color.fromRGBO(7, 94, 84, 1),
  ];
}
